---
language: node_js

node_js:
  - "10"

jobs:
  include:
  - stage: test
    script:
      - echo "$RELEASE_IP_ADDR"
      - npm test
    if: type = pull_request
  - stage: deploy
    script:
      - npm test
      - ./.travis/deploy.sh
    if: type = push AND branch = master
