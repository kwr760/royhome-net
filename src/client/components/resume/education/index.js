// @flow

import React from 'react';
import type { ResumeType } from '../../../store/resume/resume.types';

const ResumeEducation = ({ resume }: {| resume: ResumeType |}) => (
  <div>
    Resume Education
  </div>
);

export default ResumeEducation;
