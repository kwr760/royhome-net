// @flow

import React from 'react';
import type { ResumeStateType } from '../../../store/resume/resume.types';

const ResumeEducation = ({ resume }: {| resume: ResumeStateType |}) => (
  <div>
    Resume Education
  </div>
);

export default ResumeEducation;
