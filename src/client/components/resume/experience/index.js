// @flow

import React from 'react';
import type { ResumeStateType } from '../../../store/resume/resume.types';

const ResumeExperience = ({ resume }: {| resume: ResumeStateType |}) => (
  <div>
    Resume Experience
  </div>
);

export default ResumeExperience;
